apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: invalid-pvc
  namespace: 234567-1-app
  labels:
    tenant_nar_id: "234567-1"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  dataSource:
    name: wrong-class-snapshot
    kind: VolumeSnapshot
    apiGroup: snapshot.storage.k8s.io
